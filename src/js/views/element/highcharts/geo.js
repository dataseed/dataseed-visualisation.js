define(['backbone', 'underscore'],
    function(Backbone, _) {
    'use strict';

    var GeoChartView = Backbone.View.extend({

        render: function() {
            this.$el.text('Not implemented');
            return this;
        }

    });

    return GeoChartView;

});
